services:
  iss-tracker:
    build: .
    container_name: iss-tracker
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - TZ=America/Chicago
    volumes:
      - tle_cache:/app/tle_cache

volumes:
  tle_cache:

